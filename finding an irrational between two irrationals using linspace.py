import numpy as np
import math as mt
a1=np.linspace(mt.sqrt(2),mt.sqrt(3),8) #first and last values are always
                                        #start and stop, so for 1 item in b/w
                                        #2 write 3 items to generate
print(a1)